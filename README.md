# Devlog

This is Juyoung's personal project to create a web of simple artifacts for developers.

### 0.0.8 (Current)

- /articles/add
  image handler update.

- /articles/[id]
  article detail page HTML insert.
  (should be prevent XSS)
